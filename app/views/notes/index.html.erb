
<% if current_user %>


  <% @notes.each do |note| %>
    <h2>
      <%= link_to note.title, note %>
    </h2>
    <p>
      <%= time_ago_in_words(note.created_at) %>
    </p>
      <%= link_to "New Note", new_note_path %>
    <% end %>
  <% end %>
